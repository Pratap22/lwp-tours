/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'randomuser.me',
      'images.unsplash.com',
    ],
  },
};

export default nextConfig;
